import React, { Component } from 'react'
import { connect } from 'react-redux'

class GenresContainer extends Component {
  render() {
    return (
      <div>
        
      </div>
    )
  }
}

export default connect()(GenresContainer)
